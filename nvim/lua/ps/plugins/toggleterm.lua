return {
  {
    'akinsho/toggleterm.nvim',
    version = "*",
    opts = {
      -- Always be in insert mode when entering terminal window.
      start_in_insert = true,
      persist_mode = false,
    },
  }
}
